name: Manual Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version bump type'
        required: true
        type: choice
        default: patch
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Generate app token
        id: app-token
        uses: actions/create-github-app-token@v3
        with:
          client-id: ${{ secrets.RELEASE_APP_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          permission-contents: write

      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
          token: ${{ steps.app-token.outputs.token }}

      - name: Calculate next version
        id: next
        env:
          BUMP: ${{ inputs.bump }}
        run: |
          CURRENT=$(jq -r '.version' package.json)
          IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT"

          case "$BUMP" in
            major) MAJOR=$((MAJOR + 1)); MINOR=0; PATCH=0 ;;
            minor) MINOR=$((MINOR + 1)); PATCH=0 ;;
            patch) PATCH=$((PATCH + 1)) ;;
          esac

          NEXT="${MAJOR}.${MINOR}.${PATCH}"
          echo "version=$NEXT" >> "$GITHUB_OUTPUT"
          echo "Bumping $CURRENT → $NEXT ($BUMP)"

      - name: Update version
        env:
          NEXT: ${{ steps.next.outputs.version }}
        run: |
          jq --arg v "$NEXT" '.version = $v' package.json > tmp.json && mv tmp.json package.json
          echo "Updated package.json → $NEXT"

      - name: Generate release notes
        run: |
          bash .github/scripts/generate-notes.sh HEAD > /tmp/release-notes.md

      - name: Update changelog
        env:
          NEXT: ${{ steps.next.outputs.version }}
        run: bash .github/scripts/update-changelog.sh "$NEXT" /tmp/release-notes.md

      - name: Commit, tag, and push
        env:
          NEXT: ${{ steps.next.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "release: v${NEXT}"
          git tag "v${NEXT}"
          git push origin HEAD --tags

      - name: Build artifact
        run: |
          echo "Building vault-onboarding.zip..."
          zip -r vault-onboarding.zip SKILL.md references/ assets/ README.md LICENSE

      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
          NEXT: ${{ steps.next.outputs.version }}
        run: |
          gh release create "v${NEXT}" vault-onboarding.zip \
            --title "v${NEXT}" \
            --notes-file /tmp/release-notes.md
